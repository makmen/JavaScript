function Game() {    var limit = 90;    var numbers = [];    var position = 0;    fillNumbers();        this.numbers = function() {        return numbers;    };        this.position = function() {        return position;    };        this.nextStep = function() {        return numbers[position++];    };        function shuffle(array) {        var currentIndex = array.length, temporaryValue, randomIndex;        while (0 !== currentIndex) {            randomIndex = Math.floor(Math.random() * currentIndex);            currentIndex -= 1;            temporaryValue = array[currentIndex];            array[currentIndex] = array[randomIndex];            array[randomIndex] = temporaryValue;        }        return array;    }        function fillNumbers() {        var arr = [];        for(var i = 1; i <= limit; i++) {            arr.push(i);        }        numbers = shuffle(arr);    }}function Ticket() {    var numbers = [];    var checkNumbers = [];    var limit = 90;    var limitTicket = 30;    var limitInRow = 5;    var win = false;    fillTicket();        this.numbers = function() {        return numbers;    };        this.checkNumbers = function() {        return checkNumbers;    };        this.markNumber = function(num) {        checkNumbers.push(num);        if (checkNumbers.length >= limitTicket) {            win = true;        }                return win;    };        this.getElements = function(pos) {        var begin = (pos * limitInRow);        var end = (pos * limitInRow) + limitInRow;                return numbers.slice(begin, end);    };      function fillTicket() {        var enough = true;        var k = 0;        while(enough) {            k = Math.floor( Math.random() * limit + 1 );            if (!inArray(numbers,  k)) {                numbers.push(k);            }            if (numbers.length >= limitTicket) {                enough = false;            }        }    }        function inArray(arr, k) {        var success = false;        for(var i = 0; i < arr.length; i++) {            if (arr[i] == k) {                success = true;                break;            }        }                return success;    }   }